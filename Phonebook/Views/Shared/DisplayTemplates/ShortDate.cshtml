@*
    This partial view defines DateTimeOffset display format for 
    DisplayFor helper in Index view
*@

@model System.DateTimeOffset
@(Model.Year==1 ? "brak danych" : @Model.ToString("dd MMMM yyyy"))
